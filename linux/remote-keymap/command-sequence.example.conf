# One shell command per line. Blank lines and lines beginning with # are ignored.
# The bridge pastes the next line into the focused terminal and presses Enter.
# If the focused window is not a terminal, the command is ignored for safety.
# Editing this file resets the next command to the first line.
# Example:
# notify-send 'remote command 1'
# notify-send 'remote command 2'
